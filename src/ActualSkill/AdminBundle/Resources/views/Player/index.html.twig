{% extends 'ActualSkillAdminBundle::admin.html.twig' %}

{% block body %}

<h1>Player list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Slug</th>
            <th>Firstname</th>
            <th>Lastname</th>
            <th>Nickname</th>
            <th>Birthday</th>
            <th>Height</th>
            <th>Weight</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in players %}
        <tr>
            <td><a href="{{ path('admin_player_show', { 'id': entity.slug }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.slug }}</td>
            <td>{{ entity.firstname }}</td>
            <td>{{ entity.lastname }}</td>
            <td>{{ entity.nickname }}</td>
            <td>{{ entity.birthday|date('Y-m-d H:i:s') }}</td>
            <td>{{ entity.height }}</td>
            <td>{{ entity.weight }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_player_show', { 'id': entity.slug }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_player_edit', { 'id': entity.slug }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('admin_player_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}