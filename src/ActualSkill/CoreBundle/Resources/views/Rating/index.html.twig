<h1>Rating list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Rating</th>
            <th>Modified_at</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('rating_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.rating }}</td>
            <td>{% if entity.modifiedat %}{{ entity.modifiedat|date('Y-m-d H:i:s') }}{% endif%}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('rating_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('rating_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('rating_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
